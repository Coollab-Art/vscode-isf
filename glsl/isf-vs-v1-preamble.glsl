// ── ISF v1 vertex shader specific ────────────────────────────────────────────

/** ISF v1 vertex shader initializer. Call this at the start of main() — sets gl_Position and vv_FragNormCoord. */
void vv_vertShaderInit();
