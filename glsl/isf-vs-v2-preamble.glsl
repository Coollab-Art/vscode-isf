// ── ISF v2 vertex shader specific ────────────────────────────────────────────

/** ISF vertex shader initializer. Call this at the start of main() — sets gl_Position and isf_FragNormCoord. */
void isf_vertShaderInit();
